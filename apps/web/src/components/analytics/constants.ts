export const distanceChartConfig = {
  distance: {
    label: "Distance",
    color: "var(--primary)",
  },
} as const;

export const prChartConfig = {
  duration: {
    label: "PR time",
    color: "var(--primary)",
  },
} as const;
